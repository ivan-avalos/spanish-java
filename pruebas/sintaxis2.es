entero a = 10;
entero b = 20;
booleano c = verdadero;
caracter d;
funcion entero a (entero a, cadena b) {
    b = ((40 - 2) + 10) * 2 + b(10);
    imprimir ("Hola");
    si (a >= 10) {
        imprimir (b + 20);
    } sino b;
    leer d;
    retornar 10 + 20;
};
funcion caracter b (booleano a) {
    imprimir ("Adiós");
    mientras (a < 10) {
        imprimir ("Hola");
        si (a >= 10) {
            continuar;
        } sino detener;
        a = a + 1;
    };
    leer x;
    retornar;
};